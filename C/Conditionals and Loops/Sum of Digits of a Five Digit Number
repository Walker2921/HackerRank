#include<stdio.h>
#include<stdlib.h>
int main()
{
    int a,i,sum=0;
    scanf("%d",&a);
    while(a>0)
    {
        sum = sum + (a%10);
        a = a/10;
    }
    printf("%d",sum);
    return 0;
}
